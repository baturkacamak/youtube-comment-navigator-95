// src/services/comments/fetchComments.ts
export const fetchChatReplies = async (_pageToken = '') => {
  // This is a dummy function that returns an empty list of chat replies
  return {
    items: [],
  };
};
